
<div class="card mx-auto border-primary p-4" style="width: 24rem">
{{ form_start(form) }}
    {{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.name,{ 'label': 'Nom' }) }}
    {{ form_row(form.firstname,{ 'label': 'Prénom' }) }}
    {{ form_row(form.email,{ 'label': 'Email' }) }}
    {{ form_row(form.description,{ 'label': 'Description de la demande' }) }}

    <button class="btn  ">{{ button_label|default('Soumettre') }}</button>


{{ form_end(form) }}

</div>
